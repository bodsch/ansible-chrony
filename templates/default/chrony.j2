#jinja2: trim_blocks: True, lstrip_blocks: True
# {{ ansible_managed }}

# Options to pass to chrony.
DAEMON_OPTS="-F 1 -l {{ chrony_config.log.directory }}/chrony.log -f {{ chrony_config_file }} -r"
